const title="Palindrome";
const url="palindrome";
const summary="Given a string, determine if it is a palindrome.";
const details="A string is a palindrome if reading it forwards and backwards the value is the same.<br><br>Strings as input:<br>To read strings as input repeatedly call the <b>INPUT</b> ecall. The string will be read one character as a time, and will be null-terminated. If the input is italicized, that indicated is is provided as a string.";
const constraints="The string will only consist of uppercase letters, lowercase letters, and numbers. The length of the string will not exceed 255 characters.";
const difficulty=['Intermediate', 'gold', '#ff8'];
const input_type = "str";
const tests=[[['racecar'], 1], [['sand'], 0], [[''], 1], [['I'], 1], [['Wow'], 0], [['1331'], 1], [['111111111111'], 1], [['32'], 0]];